diff --git a/more_itertools/more.py b/more_itertools/more.py
index 737b07d..ebe7a1c 100755
--- a/more_itertools/more.py
+++ b/more_itertools/more.py
@@ -2405,14 +2405,10 @@ class numeric_range(Sequence):
         )
 
     def __reversed__(self):
-        # Empty iterator
-        try:
-            start = self._get_by_index(-1)
-        except IndexError:
-            return iter([])
-
         return iter(
-            numeric_range(start, self._start - self._step, -self._step)
+            numeric_range(
+                self._get_by_index(-1), self._start - self._step, -self._step
+            )
         )
 
     def count(self, value):
